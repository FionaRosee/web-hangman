name: CI //after running 2

on: [ push ]

jobs:
  test:
    runs-on: unbuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 16
        uses: actions/setup-java@v2
        with:
          distribution: "adopt"
          jave-version: 16
      - name: clean build
        run: ./gradlew clean test --info --stacktrace